import { genTypes as genTypes_ } from './gen-types'

export const genTypes = genTypes_
export const merge = genTypes_

import { genPaths as genPaths_ } from './gen-paths'

export const genPaths = genPaths_